# Core runtime dependencies
pydantic>=2.7
python-dotenv>=1.0

# Document ingestion
pymupdf>=1.24          # PDF text/table extraction (imported as `fitz`)
python-pptx>=1.0       # PPTX extraction

# PDF rendering + verification
reportlab>=4.0
pypdf>=4.0

# LLM providers (install at least one)
anthropic>=0.40
openai>=1.40

# Optional UI
streamlit>=1.35

# Optional OCR fallback (also requires the Tesseract binary on PATH)
# pytesseract>=0.3.10
# pillow>=10.0

# Development
pytest>=8.0
